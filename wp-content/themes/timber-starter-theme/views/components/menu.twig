<div class="menu">
  <button class="menu-burger">
    <div></div>
    <div></div>
    <div></div>
  </button>
  <nav class="menu-mobile">
      <ul>
      {% for item in taxonomy_nav.items %}
        <li class="menu-taxonomy" ><a href="{{item.url}}">{{item.title}}</a></li>
      {% endfor %}
      </ul>
      <ul>
      {% for item in main_nav.items %}
        <li><a href="{{item.url}}">{{item.title}}</a></li>
      {% endfor %}
      </ul>
  </nav>
</div>


